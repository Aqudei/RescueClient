<UserControl x:Class="RescueApp.Views.People"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RescueApp.Views"
             xmlns:maha="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" DataContext="{Binding PeopleVM, Source={StaticResource Locator}}"
             d:DesignHeight="800.339" d:DesignWidth="797.456">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="1" Orientation="Horizontal"
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Bottom" Margin="0,0,8,8">
            <Button Command="{Binding RegisterCommand}">Register Person</Button>
        </StackPanel>

        <ScrollViewer x:Name="scrollViewer" HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="peopleList" ItemsSource="{Binding People}" 
                ScrollViewer.CanContentScroll="True">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="1" Width="400">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Image Width="128" Margin="8"
                                Height="128"
                                Source="{Binding Photo}" />

                            <StackPanel Grid.Column="1" Orientation="Vertical" Margin="5">
                                <TextBlock FontWeight="Bold" 
                                    Text="{Binding FullName}" FontSize="18" />
                                <TextBlock FontStyle="Italic" 
                                    Text="{Binding Address}" FontSize="12" />

                                <StackPanel Orientation="Horizontal">

                                    <TextBlock Text="Age: " />
                                    <TextBlock Text="{Binding Age}" />
                                    <TextBlock Text="|" Margin="4,0" />
                                    <TextBlock Text="Blood Type: " />
                                    <TextBlock Text="{Binding BloodType}" />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal">
                                    <Button HorizontalAlignment="Left" Margin="4,8" Padding="8"
                                    Style="{DynamicResource MahApps.Metro.Styles.MetroCircleButtonStyle}"
                                    Command="{Binding DataContext.DeleteCommand, ElementName=peopleList, Mode=OneWay}" 
                                    CommandParameter="{Binding Mode=OneWay}">
                                        <iconPacks:PackIconMaterial Kind="Delete"/>
                                    </Button>

                                    <Button HorizontalAlignment="Left" Margin="4,8" Padding="8"
                                    Style="{DynamicResource MahApps.Metro.Styles.MetroCircleButtonStyle}">
                                        <iconPacks:PackIconMaterial Kind="AccountEdit"/>
                                    </Button>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
