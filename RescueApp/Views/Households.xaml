<UserControl x:Class="RescueApp.Views.Households"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RescueApp.Views"
             DataContext="{Binding HouseholdsVM, Source={StaticResource Locator}}"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ListView ItemsSource="{Binding Households}" ItemTemplate="{DynamicResource DataTemplate1}">
            <ListView.Resources>
                <DataTemplate x:Key="DataTemplate1">
                    <Grid>
                        <TextBlock Text="{Binding HouseNumber, Mode=TwoWay}" 
                                   TextWrapping="Wrap" />
                    </Grid>
                </DataTemplate>
            </ListView.Resources>

        </ListView>
    </Grid>
</UserControl>
